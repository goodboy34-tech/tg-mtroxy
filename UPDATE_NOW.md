# 🚀 ОБНОВЛЕНИЕ СЕРВЕРА - КРИТИЧЕСКИЙ ФИКС

## ❌ Была ошибка:
```
Failed to connect to node: Node API error: 500 - 
{"error":"ENOENT: no such file or directory, open '/docker-compose.yml'"}
```

## ✅ Что исправлено:
Убраны лишние вызовы `restartSocks5()` после `updateSocks5Config()`, которые пытались перезапустить несуществующий контейнер.

---

## 📦 Команды для обновления:

### На сервере с нодами (где node-agent):
```bash
# 1. Перейти в директорию
cd /root/eeee  # или где у вас репозиторий

# 2. Обновить код
git pull origin master

# 3. Пересобрать node-agent
docker compose build node-agent

# 4. Перезапустить node-agent
docker compose up -d --force-recreate node-agent

# 5. Проверить логи
docker logs -f mtproxy-node-agent
```

### На сервере с control-panel (если отдельный):
```bash
cd /root/eeee
git pull origin master
docker compose build control-panel
docker compose up -d --force-recreate control-panel
docker logs -f mtproxy-control-panel
```

---

## ✅ Тест после обновления:

1. **Попробовать создать SOCKS5:**
   - Открыть бота
   - "🔗 Управление ссылками" → выбрать ноду → "➕ SOCKS5"
   - Отправить:
     ```
     username: test123
     password: pass456
     ```
   - Должна появиться ссылка **БЕЗ ОШИБКИ**

2. **Проверить контейнер:**
   ```bash
   docker ps | grep socks5
   # Должен быть запущен mtproxy-socks5
   ```

3. **Проверить подключение:**
   ```bash
   curl -x socks5://test123:pass456@localhost:1080 https://ifconfig.me
   # Должен вернуть IP сервера
   ```

---

## 🔍 Если всё ещё ошибка:

```bash
# Посмотреть логи node-agent
docker logs mtproxy-node-agent | tail -50

# Проверить что образ обновился
docker images | grep node-agent

# Принудительно пересоздать
docker compose down
docker compose up -d
```

---

## 📝 Что изменилось в коде:

**Файл:** `node-agent/src/api.ts`

**Было:**
```typescript
await updateSocks5Config(accounts);
await restartSocks5();  // ❌ Лишний вызов!
```

**Стало:**
```typescript
await updateSocks5Config(accounts);  // ✅ Достаточно этого
```

Функция `updateSocks5Config()` уже:
- Останавливает старый контейнер
- Удаляет старый контейнер
- Создаёт новый с обновлёнными аккаунтами

Поэтому `restartSocks5()` был избыточен и вызывал ошибку.

---

Commit: `03ebca8`
