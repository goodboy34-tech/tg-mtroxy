# Последние обновления проекта

## Обзор изменений

### 1. SOCKS5 с авторизацией (3proxy)
**Изменено:** Замена Dante на 3proxy для поддержки аутентификации username/password

**Файлы:**
- `docker-compose.node.yml` - обновлён образ на `tarampampam/3proxy:latest`
- `node-agent/src/api.ts` - функция `updateSocks5Config()` генерирует конфиг в формате 3proxy

**Формат конфига 3proxy:**
```
log /dev/stdout
nscache 65536
timeouts 1 5 30 60 180 1800 15 60
internal 0.0.0.0
external 0.0.0.0

users username1:CL:password1
users username2:CL:password2

auth strong
socks -p1080
flush
```

**Применение:**
1. При добавлении SOCKS5 аккаунта через `/add_socks5`, Node Agent генерирует новый конфиг
2. Конфиг сохраняется в `socks5/3proxy.cfg`
3. Docker контейнер монтирует этот файл и применяет настройки

---

### 2. Система подписок для 4000+ пользователей

**Новый файл:** `src/subscription-manager.ts`

**Классы:**
- `SubscriptionManager` - управление подписками
- `SubscriptionFormatter` - форматирование для отображения

**Возможности:**
- ✅ Создание подписок с выбором нод
- ✅ Включение/выключение MTProto и SOCKS5 в подписке
- ✅ Генерация готовых ссылок tg:// и socks5://
- ✅ Экспорт JSON для импорта в Telegram
- ✅ Активация/деактивация подписок
- ✅ Удаление подписок
- ✅ Статистика по использованию

**Команды бота:**
```
/create_subscription <название> - создать подписку
/subscriptions - список всех подписок
/subscription <id> - детали подписки с кнопками:
  📥 JSON для импорта
  🔄 Обновить
  ⏸/▶️ Деактивировать/Активировать
  🗑 Удалить
```

**База данных:**
Таблица `subscriptions` в `src/database.ts`:
- `id` - уникальный ID подписки
- `name` - название
- `description` - описание
- `node_ids` - JSON массив ID нод
- `include_mtproto` - включить MTProto (0/1)
- `include_socks5` - включить SOCKS5 (0/1)
- `subscription_url` - уникальная ссылка (для API)
- `is_active` - активна ли подписка
- `access_count` - счётчик обращений
- `last_accessed` - последнее обращение
- `created_at`, `updated_at` - временные метки

**Пример использования:**
1. Админ создаёт подписку: `/create_subscription VIP Доступ`
2. Выбирает ноды (можно несколько)
3. Подписка генерирует список всех прокси на этих нодах
4. Админ получает:
   - Готовые ссылки для копирования
   - JSON файл для импорта в Telegram
5. Админ может отключить подписку, и все прокси станут недоступны

---

### 3. Масштабируемость для 4000 пользователей

**Анализ:**
- 1 нода с 4 воркерами = ~1000-1500 одновременных подключений
- 4000 пользователей требуют 3-4 ноды
- SQLite поддерживает миллионы записей с индексами

**Рекомендации:**
1. Использовать 3-4 ноды в разных локациях
2. По 4 воркера на ноду (по количеству CPU ядер)
3. Распределять пользователей через подписки
4. Мониторить нагрузку через `/stats`

**Пример конфигурации:**
```
Нода 1 (Германия):  1500 пользователей
Нода 2 (Нидерланды): 1500 пользователей
Нода 3 (США):        1000 пользователей
──────────────────────────────────────────
Итого:              4000 пользователей
```

---

### 4. Домены для MTProto

**Как это работает:**

1. **DNS настройка:**
   ```
   proxy1.example.com  A  1.2.3.4
   ```

2. **Конфиг ноды (.env):**
   ```bash
   DOMAIN=proxy1.example.com
   INTERNAL_IP=10.0.0.5  # Если NAT
   ```

3. **MTProxy запускается с флагом:**
   ```bash
   --nat-info INTERNAL_IP:DOMAIN
   ```

4. **Клиенты подключаются:**
   ```
   tg://proxy?server=proxy1.example.com&port=443&secret=dd...
   ```

**Важно:** 
- Домен должен указывать на IP сервера (DNS A запись)
- В .env указать DOMAIN и INTERNAL_IP (если за NAT)
- MTProxy автоматически получит флаг --nat-info

---

### 5. Обновлённая справка бота

Команда `/help` теперь включает раздел "Подписки":

```
/create_subscription <название> - создать подписку
/subscriptions - список всех подписок  
/subscription <id> - детали подписки
```

---

## Файлы, требующие внимания

### ⚠️ Требуют доработки:

1. **Интерактивное добавление ноды** (`/add_node`)
   - Сейчас: показывает инструкцию
   - Нужно: добавить Telegraf Scenes/Wizard для диалога
   - Собрать: name, domain, IP, ports, workers, CPU, RAM

2. **Команда `/add_socks5`**
   - Сейчас: не реализована
   - Нужно: аналог `/add_secret` для SOCKS5
   - Генерировать username/password, отправлять на Node Agent

3. **Команда `/logs`**
   - Сейчас: не реализована
   - Нужно: вызов `GET /system/logs` на Node Agent

4. **Команда `/set_workers`**
   - Сейчас: не реализована
   - Нужно: изменение WORKERS в контейнере MTProxy

### ✅ Готово к использованию:

- `/nodes` - список нод
- `/node <id>` - детали ноды
- `/remove_node <id>` - удаление ноды
- `/restart_node <id>` - перезапуск прокси
- `/links <node_id>` - все ссылки ноды
- `/add_secret <node_id>` - добавить MTProto секрет
- `/stats` - общая статистика
- `/health` - здоровье нод
- `/subscriptions` - управление подписками
- `/subscription <id>` - детали подписки

---

## Следующие шаги

1. **Установить зависимости:**
   ```bash
   npm install
   npm run build
   ```

2. **В node-agent тоже:**
   ```bash
   cd node-agent
   npm install
   npm run build
   ```

3. **Протестировать на реальных серверах:**
   - Control Panel: выполнить `scripts/setup-control.sh`
   - Ноды: выполнить `scripts/setup-node.sh`

4. **Создать первую подписку:**
   - Добавить ноды через `/add_node`
   - Добавить секреты через `/add_secret`
   - Создать подписку `/create_subscription`
   - Проверить работу прокси

---

## Технические детали

### TypeScript ошибки в IDE
Ошибки о `Cannot find name 'process'`, `'Buffer'`, `'console'` исчезнут после:
```bash
npm install  # Установит @types/node
```

### Архитектура подписок
```
Подписка "VIP"
├── Нода 1 (Германия)
│   ├── MTProto Secret 1 (dd)
│   ├── MTProto Secret 2 (normal)
│   └── SOCKS5 Account 1 (user:pass)
├── Нода 2 (США)
│   ├── MTProto Secret 3 (dd)
│   └── SOCKS5 Account 2 (user:pass)
└── Экспорт:
    ├── tg://proxy?server=node1.com&port=443&secret=dd...
    ├── tg://proxy?server=node1.com&port=443&secret=ee...
    ├── socks5://user1:pass1@node1.com:1080
    ├── tg://proxy?server=node2.com&port=443&secret=dd...
    └── socks5://user2:pass2@node2.com:1080
```

### Формат JSON для Telegram импорта
```json
{
  "name": "VIP Доступ",
  "description": "Премиум подписка",
  "proxies": [
    {
      "_": "inputMediaProxyServer",
      "server": "proxy1.example.com",
      "port": 443,
      "secret": "dd1234567890abcdef..."
    },
    {
      "_": "inputMediaProxyServer",
      "server": "proxy1.example.com",
      "port": 1080,
      "username": "user123",
      "password": "pass456",
      "type": "socks5"
    }
  ]
}
```

---

## Changelog

**[2024-XX-XX] Версия 2.0**
- ✅ Добавлена система подписок
- ✅ SOCKS5 с авторизацией (3proxy)
- ✅ Генерация JSON для импорта в Telegram
- ✅ Поддержка доменов через --nat-info
- ✅ Оптимизация для 4000+ пользователей
- ✅ Обновлена документация

**[Предыдущая версия] Версия 1.0**
- Базовое управление нодами
- MTProto прокси
- SOCKS5 без авторизации (Dante)
- Мониторинг и статистика
