# 3proxy Configuration
# Generated automatically by Node Agent

# Basic configuration
nserver 8.8.8.8
nserver 8.8.4.4
nscache 65536

# Disable daemon mode to prevent PID file creation
# daemon

# Logging
log /dev/stdout

# No PID file
# pidfile /var/run/3proxy.pid
# rotate 30

# SOCKS5 proxy with authentication on port 1080
# Users will be added dynamically by the node agent
socks -p1080

# Allow authenticated users only
allow *

# End of configuration
