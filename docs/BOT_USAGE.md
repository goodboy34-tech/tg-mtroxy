# Использование Telegram бота

## 🎯 Главное меню

При запуске бота (`/start`) отображается главное меню с кнопками:

- **📡 Ноды** — управление прокси-нодами
- **📦 Подписки** — управление подписками
- **👤 Пользователи MTProto** — управление пользователями
- **➕ Создать MTProto** — создание MTProto для пользователя
- **📊 Статистика** — общая статистика системы
- **⚙️ Настройки** — дополнительные настройки

## 📡 Управление нодами

### Просмотр списка нод
Нажмите **📡 Ноды** в главном меню или отправьте `/nodes`

Отображается список всех нод с их статусом:
- 🟢 — онлайн
- 🔴 — офлайн
- 🟡 — неизвестно

### Информация о ноде
Нажмите на ноду в списке для просмотра детальной информации:
- Статус и здоровье
- CPU, RAM, Disk
- Статистика подключений
- Трафик

**Доступные действия:**
- 🔄 Перезапустить — перезапуск прокси на ноде
- 🗑 Удалить — удаление ноды

### Добавление ноды
Нажмите **➕ Добавить ноду** или отправьте `/add_node`

Заполните данные:
- Название ноды
- Домен
- IP адрес
- API URL (например: `http://10.0.0.1:8080`)
- API Token (из `.env` ноды)

## 📦 Управление подписками

### Просмотр списка подписок
Нажмите **📦 Подписки** в главном меню или отправьте `/subscriptions`

### Информация о подписке
Нажмите на подписку в списке для просмотра:
- Список прокси (MTProto и SOCKS5)
- Готовые ссылки для импорта
- Статистика обращений

**Доступные действия:**
- 📥 JSON для импорта — экспорт подписки
- 🔄 Обновить — обновление данных
- ⏸ Деактивировать / ▶️ Активировать — изменение статуса
- 🗑 Удалить — удаление подписки
- 👥 Пользователи — список пользователей подписки

## 👤 Пользователи MTProto

### Поиск пользователя
Нажмите **👤 Пользователи MTProto** → **🔍 Поиск**

Можно искать по:
- Секрету MTProto (например: `dd1234567890abcdef`)
- Telegram ID (например: `123456789`)
- UUID пользователя (например: `abc-def-ghi`)

### Информация о пользователе
Отображается:
- Привязки Remnawave
- Активные MTProto секреты
- Ссылки для подключения

**Доступные действия:**
- ❌ Отключить доступ — удаление всех секретов пользователя

## ➕ Создание MTProto из бота

Нажмите **➕ Создать MTProto** в главном меню.

### По ссылке Remnawave
1. Выберите **🔗 По ссылке Remnawave**
2. Отправьте ссылку на подписку (например: `https://panel.example.com/subscription/abc123`)
3. Бот создаст MTProto секреты и вернёт ссылки

### По Telegram ID
1. Выберите **🆔 По Telegram ID**
2. Отправьте Telegram ID пользователя (например: `123456789`)
3. Бот проверит подписку в Remnawave и создаст MTProto

### По Username
1. Выберите **👤 По Username**
2. Отправьте username без @ (например: `username`)
3. Бот найдёт пользователя и создаст MTProto

### По UUID
1. Выберите **🆔 По UUID**
2. Отправьте UUID пользователя из Remnawave
3. Бот создаст MTProto для пользователя

## 📊 Статистика

Нажмите **📊 Статистика** в главном меню или отправьте `/stats`

Отображается:
- Количество активных нод
- Статистика по каждой ноде:
  - Подключения MTProto/SOCKS5
  - CPU, RAM
  - Трафик (входящий/исходящий)
- Общая статистика:
  - Всего подключений
  - Общий трафик
  - Количество пользователей

## ⚙️ Настройки

### Здоровье нод
Нажмите **⚙️ Настройки** → **🏥 Здоровье нод** или отправьте `/health`

Отображается статус всех нод и их здоровье.

### Логи
Нажмите **⚙️ Настройки** → **📋 Логи** или отправьте `/logs <node_id>`

Просмотр логов MTProto и SOCKS5 контейнеров.

## 💡 Полезные команды

Все команды также доступны через текстовые команды:

- `/start` — главное меню
- `/nodes` — список нод
- `/node <id>` — информация о ноде
- `/subscriptions` — список подписок
- `/subscription <id>` — информация о подписке
- `/stats` — статистика
- `/health` — здоровье нод
- `/user_mtproxy <telegram_id>` — информация о пользователе
- `/search_mtproxy <секрет|id|uuid>` — поиск
- `/disable_mtproxy <telegram_id>` — отключить доступ

## 🔄 Навигация

В каждом меню есть кнопка **🔙 Главное меню** для возврата к главному экрану.

Кнопка **🔙 К списку** возвращает к списку (нод/подписок).

