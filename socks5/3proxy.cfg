# 3proxy Configuration
# Generated automatically by Node Agent

# Disable logging to files
nolog

# DNS servers
nserver 8.8.8.8
nserver 8.8.4.4

# SOCKS5 proxy on port 1080
socks -p1080

# Allow authenticated users only
allow *uration
# Minimal configuration for SOCKS5 proxy

# Disable logging
nolog

# DNS servers
nserver 8.8.8.8
nserver 8.8.4.4

# SOCKS5 proxy on port 1080
socks -p1080

# Allow all connections (authentication will be added by node agent)
allow *

# End of configuration
