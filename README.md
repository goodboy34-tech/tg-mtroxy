# tg-mtproxy — Система управления MTProto прокси

Профессиональная система управления MTProto/SOCKS5 прокси-серверами с интеграцией Remnawave для автоматической выдачи доступа пользователям с активными подписками.

## 🏗️ Архитектура

```
┌─────────────────┐
│  Control Panel  │ ← Telegram Bot (админка)
│  (Центральный   │ ← Remnawave API (интеграция)
│   сервер)       │ ← Web API (для веб-приложения)
└────────┬────────┘
         │
         ├─── Node Agent 1 ───→ MTProto Container
         ├─── Node Agent 2 ───→ MTProto Container
         └─── Node Agent N ───→ MTProto Container
```

### Компоненты

- **`control-panel/`**: Центральный сервис управления
  - Telegram бот для администрирования
  - Remnawave API для интеграции с панелью управления Xray
  - Web API для веб-приложения
  - База данных SQLite для хранения нод, подписок и привязок
  - **Redis** для кэширования (обязателен для продакшена с тысячами пользователей)
  - Профессиональное логирование с ротацией файлов (Winston)
  - Автоматическая проверка статусов подписок (каждые 30 минут)
  - Автоматическая самоочистка старых данных, логов и Docker образов

- **`node-agent/`**: Агент на каждой прокси-ноде
  - Управление Docker-контейнерами MTProto и SOCKS5
  - Сбор метрик (CPU, RAM, трафик, подключения)
  - Health checks и статистика
  - REST API для управления секретами

## 🚀 Быстрый старт

### 📍 Что устанавливать где?

**На МЕЙН-СЕРВЕР (Control Panel):**
- Устанавливается `control-panel/` — центральный сервис управления
- Содержит: Telegram бот, базу данных, Remnawave API, Web API
- **Файлы:** `install-control.sh`, `docker-compose.yml`, `control-panel/`

**На НОДУ (Node Agent):**
- Устанавливается `node-agent/` — агент на каждой прокси-ноде
- Содержит: управление Docker контейнерами MTProto/SOCKS5, сбор метрик
- **Файлы:** `install-node.sh`, `docker-compose.node.yml`, `node-agent/`

### Установка Control Panel (на МЕЙН-СЕРВЕР)

```bash
# На главном сервере управления
git clone <ваш-репозиторий>
cd tg-mtproxy
cp ENV.example .env
# Отредактируйте .env файл (BOT_TOKEN, ADMIN_IDS, REMNAWAVE_API_KEY и т.д.)
./install-control.sh
```

**Важно:** На мейн-сервере нужны только переменные для `control-panel`:
- `BOT_TOKEN`, `ADMIN_IDS`
- `REMNAWAVE_API_KEY`, `WEB_API_KEY`
- `BACKEND_BASE_URL`, `BACKEND_TOKEN`
- `REDIS_HOST`, `REDIS_PORT` (Redis запускается автоматически через docker-compose)

### Установка Node Agent (на КАЖДУЮ НОДУ)

```bash
# На каждом сервере с прокси (ноде)
git clone <ваш-репозиторий>
cd tg-mtproxy
cp ENV.example .env
# Отредактируйте .env файл (API_TOKEN, DOMAIN, INTERNAL_IP и т.д.)
./install-node.sh
```

**Важно:** На ноде нужны только переменные для `node-agent`:
- `API_TOKEN` — токен для доступа к API ноды
- `DOMAIN`, `INTERNAL_IP` — сетевые настройки
- `MTPROTO_PORT`, `WORKERS` — настройки MTProto
- `MT_PROXY_IMAGE` — образ Docker для MTProto

## 💰 Настройка продаж

### YooMoney

1. Получите токен API на https://yoomoney.ru/transfer/myservices/http-notification
2. Укажите номер кошелька YooMoney
3. Добавьте в `.env`:
   ```
   YOOMONEY_TOKEN=ваш_токен
   YOOMONEY_WALLET=номер_кошелька
   ```

### Тарифы

По умолчанию создаются 4 тарифа:
- 🆓 30 минут (бесплатно, пробный период)
- ⚡ 1 день — 15 ₽ (1 нода)
- 🔵 7 дней — 50 ₽ (2 ноды)
- 🟣 30 дней — 100 ₽ (3 ноды)

Тарифы можно изменить через админ-панель бота или напрямую в базе данных.

## 📋 Переменные окружения

### Control Panel (`.env`)

```bash
# Telegram Bot
BOT_TOKEN=your_bot_token
ADMIN_IDS=123456789,987654321

# Remnawave API (для интеграции)
REMNAWAVE_API_KEY=your-secret-key
REMNAWAVE_API_PORT=8081

# Web API (для веб-приложения)
WEB_API_KEY=your-web-api-key
WEB_API_PORT=8082

# Backend API (ваш сервер с api-1.yaml)
BACKEND_BASE_URL=https://your-backend.com
BACKEND_TOKEN=your-backend-token

# Remnawave Direct API (опционально, если хотите прямой доступ)
REMNAWAVE_BASE_URL=https://panel.example.com
REMNAWAVE_TOKEN=your-remnawave-token

# Платежи (YooMoney)
YOOMONEY_TOKEN=your-yoomoney-api-token
YOOMONEY_WALLET=your-yoomoney-wallet-number

# Redis (обязательно для продакшена с тысячами пользователей)
REDIS_HOST=redis
REDIS_PORT=6379
```

### Node Agent (`.env` на ноде)

```bash
# API токен для доступа к node-agent
API_TOKEN=your-node-token

# Сетевые настройки
DOMAIN=proxy.example.com
INTERNAL_IP=10.0.0.1
MTPROTO_PORT=443
SOCKS5_PORT=1080

# MTProto настройки
WORKERS=2
AD_TAG=  # Опционально, можно оставить пустым
MT_PROXY_IMAGE=telegrammessenger/proxy:latest  # Можно использовать skrashevich/mtproxy:latest для статистики по секретам
MT_DOCKER_RUN_ARGS=--memory=512m --cpus=1.0

# SOCKS5 (опционально)
ENABLE_SOCKS5=false
SOCKS5_DOCKER_RUN_ARGS=--memory=256m
```

## 🔧 Основные возможности

### Telegram Bot команды

#### Управление нодами
- `/nodes` — список всех нод
- `/add_node` — добавить новую ноду
- `/node <id>` — информация о ноде
- `/remove_node <id>` — удалить ноду
- `/restart_node <id>` — перезапустить прокси на ноде

#### Управление подписками
- `/create_subscription <название>` — создать подписку
- `/subscriptions` — список подписок
- `/subscription <id>` — детали подписки

#### Управление пользователями MTProto
- `/user_mtproxy <telegram_id>` — информация о пользователе
- `/search_mtproxy <секрет|telegram_id|uuid>` — поиск по секрету/ID/UUID
- `/disable_mtproxy <telegram_id>` — отключить доступ пользователя
- `/subscription_mtproxy <local_subscription_id>` — информация о подписке и пользователях

#### Мониторинг
- `/stats` — общая статистика по всем нодам
- `/health` — здоровье нод
- `/logs` — последние логи

### Remnawave API

#### `POST /api/remnawave/authorize`

Авторизация пользователя и выдача MTProto ссылок.

**Запрос:**
```json
{
  "telegramId": 123456789,
  "remnawaveSubscriptionId": "sub-123",
  "localSubscriptionId": 1
}
```

**Ответ:**
```json
{
  "success": true,
  "status": "active",
  "telegramId": 123456789,
  "remnawaveUserId": "uuid-123",
  "remnawaveSubscriptionId": "sub-123",
  "localSubscriptionId": 1,
  "links": [
    "tg://proxy?server=proxy1.example.com&port=443&secret=dd..."
  ]
}
```

#### `POST /api/remnawave/users/sync`

Синхронизация пользователя с подпиской.

#### `POST /api/remnawave/subscriptions/status`

Массовое обновление статусов подписок.

### Web API

#### `POST /api/web/check-subscription`

Проверка подписки пользователя и получение MTProto ссылок (для веб-приложения).

**Запрос:**
```json
{
  "telegramId": 123456789,
  "remnawaveSubscriptionId": "sub-123"
}
```

**Ответ:**
```json
{
  "success": true,
  "hasAccess": true,
  "telegramId": 123456789,
  "remnawaveUserId": "uuid-123",
  "remnawaveSubscriptionId": "sub-123",
  "links": [
    "tg://proxy?server=proxy1.example.com&port=443&secret=dd..."
  ]
}
```

#### `GET /api/web/user-stats?telegramId=123456789`

Получение статистики по пользователю.

## 💰 Продажи MTProxy

Система поддерживает продажу MTProxy подписок через Telegram бот с оплатой через YooMoney.

### Настройка YooMoney

1. Зарегистрируйтесь на https://yoomoney.ru
2. Получите токен API: https://yoomoney.ru/transfer/myservices/http-notification
3. Укажите номер кошелька и токен в `.env`:
   ```
   YOOMONEY_TOKEN=ваш_токен
   YOOMONEY_WALLET=номер_кошелька
   ```

### Тарифы

По умолчанию создаются 4 тарифа:
- 🆓 **30 минут** — бесплатно (пробный период, 1 нода)
- ⚡ **1 день** — 15 ₽ (1 нода)
- 🔵 **7 дней** — 50 ₽ (2 ноды для стабильности)
- 🟣 **30 дней** — 100 ₽ (3 ноды для максимальной стабильности)

Тарифы можно изменить через админ-панель бота (`/admin` → `💰 Продажи` → `📋 Тарифы`).

### Приоритет доступа

1. **Купленные подписки** — имеют наивысший приоритет
2. **Remnawave подписки** — выдаются автоматически активным пользователям
3. При истечении Remnawave подписки, если есть купленная — доступ сохраняется

## 📚 Интерфейс бота

### Все действия через кнопки меню!

Бот использует **интерактивное меню с кнопками** для всех действий. Команды (`/start`, `/tariffs`, `/nodes` и т.д.) доступны для быстрого доступа, но **не обязательны** — всё можно делать через кнопки.

### Для пользователей:
- `/start` — главное меню (показывает тарифы или активные подписки)
- `/tariffs` — просмотр тарифов и покупка (также через кнопки)
- `/status` — статус подписки (также через кнопки)
- `/link` — получить MTProto ссылки (также через кнопки)

### Для администраторов:
- `/start` — главное меню с разделами
- `/nodes` — список нод (также через кнопки: 📡 Ноды)
- `/subscriptions` — список подписок (также через кнопки: 📦 Подписки)
- `/stats` — статистика системы (также через кнопки: 📊 Статистика)
- `/health` — здоровье нод (также через кнопки: ⚙️ Настройки → 🏥 Здоровье нод)

**Главное меню админа:**
- 💰 Продажи — управление тарифами и продажами
- 📡 Ноды — управление прокси-нодами
- 📦 Подписки — управление подписками
- 👤 Пользователи — поиск и управление пользователями
- ➕ Создать MTProto — создание MTProto для пользователей
- 📊 Статистика — общая статистика системы
- ⚙️ Настройки — логи, здоровье нод

## 🔐 Безопасность

- Все API требуют аутентификации через заголовок `X-API-KEY`
- Node Agent использует bearer token аутентификацию
- Персональные MTProto секреты для каждого пользователя (точечное отключение доступа)
- Автоматическая проверка статусов подписок каждые 30 минут

## 📊 Мониторинг и метрики

- **CPU и RAM** — сбор каждые 5 минут
- **Трафик** — входящий и исходящий (MB)
- **Подключения** — активные MTProto и SOCKS5 соединения
- **Статистика по пользователям** — количество активных секретов и привязок

## 🔄 Автоматизация

### Cron задачи

- **Каждую минуту**: проверка истекших подписок продаж и уведомление пользователей
- **Каждые 5 минут**: проверка здоровья нод и сбор статистики
- **Каждые 15 секунд**: проверка ожидающих платежей YooMoney
- **Каждые 30 минут**: проверка статусов Remnawave подписок и отключение доступа при истечении
- **Каждые 6 часов**: очистка старых записей в БД (истекшие подписки старше 30 дней, старые заказы)
- **Каждые 12 часов**: очистка неиспользуемых Docker образов и контейнеров
- **Ежедневно в 3:00**: полная очистка (статистика старше 7 дней, логи старше 30 дней, Docker volumes)

### Самоочистка

Система автоматически очищает:
- **База данных**: истекшие подписки старше 30 дней, завершенные/отмененные заказы старше 30 дней, метрики старше 7 дней
- **Логи**: файлы логов старше 30 дней (ротация через Winston)
- **Docker**: остановленные контейнеры старше 24 часов, неиспользуемые образы старше 7 дней, неиспользуемые volumes
- **Redis**: автоматическая очистка истекших ключей (TTL)

## 📝 Логирование

Система использует профессиональное логирование через **Winston** с автоматической ротацией файлов:

- **Консоль**: все логи в продакшене (уровень `info`), в разработке (уровень `debug`)
- **Файлы логов** (в `data/logs/`):
  - `app-YYYY-MM-DD.log` — все логи (ротация ежедневно, хранение 30 дней, макс. 50MB на файл)
  - `error-YYYY-MM-DD.log` — только ошибки (ротация ежедневно, хранение 90 дней)
  - `exceptions-YYYY-MM-DD.log` — необработанные исключения
  - `rejections-YYYY-MM-DD.log` — необработанные промисы

Логи автоматически сжимаются и ротируются. Старые логи удаляются автоматически.

## 🚀 Производительность

Система оптимизирована для работы с **тысячами пользователей**:

- **Redis кэширование**: все запросы к backend API кэшируются (TTL 2-5 минут)
- **Оптимизированная БД**: SQLite в WAL режиме для быстрого чтения
- **Load balancing**: автоматическое распределение пользователей по нодам
- **Health checks**: автоматический мониторинг состояния нод
- **Автоматическая очистка**: предотвращение роста размера БД и логов
- **Docker оптимизация**: лимиты на логи (50MB, 5 файлов), health checks, автоматическая очистка образов

## 🐳 Docker

### Control Panel

```bash
docker compose up -d
```

### Node Agent

```bash
docker compose -f docker-compose.node.yml up -d
```

## 📝 Интеграция с Remnawave

1. **Настройте backend** (ваш сервер с `api-1.yaml`):
   - Убедитесь, что доступны эндпоинты:
     - `GET /api/users/by-telegram-id/{telegramId}`
     - `GET /api/users/{uuid}/accessible-nodes`

2. **В веб-приложении**:
   - При авторизации пользователя вызывайте `POST /api/web/check-subscription`
   - Передавайте `telegramId` и `remnawaveSubscriptionId`
   - Получайте MTProto ссылки для пользователя

3. **Автоматическая синхронизация**:
   - Система автоматически проверяет статусы подписок каждые 30 минут
   - При истечении подписки доступ MTProto отключается автоматически

## 🛠️ Разработка

### Структура проекта

```
tg-mtproxy/
├── control-panel/          # Центральный сервис
│   ├── src/
│   │   ├── bot.ts         # Telegram бот
│   │   ├── database.ts    # База данных
│   │   ├── remnawave-api.ts # Remnawave API
│   │   ├── web-api.ts     # Web API
│   │   ├── backend-client.ts # Backend клиент
│   │   ├── remnawave-client.ts # Remnawave клиент
│   │   └── mtproto-user-manager.ts # Управление пользователями
│   └── Dockerfile
├── node-agent/            # Агент на ноде
│   ├── src/
│   │   └── api.ts         # REST API для управления
│   └── Dockerfile
├── docker-compose.yml     # Control Panel
├── docker-compose.node.yml # Node Agent
└── scripts/               # Скрипты установки
```

### Сборка

```bash
# Control Panel
cd control-panel
npm install
npm run build

# Node Agent
cd node-agent
npm install
npm run build
```

## 📚 Дополнительная документация

- [QUICK-START.md](./QUICK-START.md) — быстрый старт
- [docs/backend-contract.md](./docs/backend-contract.md) — контракт API с backend

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи контейнеров: `docker logs mtproxy-control` или `docker logs mtproxy-node-agent`
2. Проверьте файловые логи: `tail -f data/logs/app-$(date +%Y-%m-%d).log`
3. Убедитесь, что все переменные окружения установлены
4. Проверьте доступность нод через `/health` в боте
5. Проверьте статус Redis: `docker logs mtproxy-redis`
6. Проверьте здоровье контейнеров: `docker ps` (должны быть `healthy`)

## 📄 Лицензия

MIT
