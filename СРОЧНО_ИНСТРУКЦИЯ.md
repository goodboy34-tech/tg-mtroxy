# СРОЧНО! Ошибка на сервере - как исправить

## Проблема:
```
❌ Ошибка при добавлении: Failed to connect to node Node-Moscow: 
Node API error: 500 - {"error":"ENOENT: no such file or directory, 
open '/docker-compose.yml'"}
```

## Причина:
**На сервере запущена старая версия кода!** Node-agent всё ещё пытается редактировать `docker-compose.yml`, хотя в новой версии это исправлено.

## Решение:

### Вариант 1: Автоматический скрипт

Скопируйте `UPDATE_SERVER.sh` на сервер и запустите:

```bash
# На локальном компьютере
scp UPDATE_SERVER.sh root@YOUR_SERVER_IP:/root/

# На сервере
ssh root@YOUR_SERVER_IP
cd /root
chmod +x UPDATE_SERVER.sh
./UPDATE_SERVER.sh
```

### Вариант 2: Вручную

Выполните команды на сервере:

```bash
# 1. Перейти в директорию проекта
cd /root/eeee  # или где у вас проект

# 2. Остановить контейнеры
docker compose down

# 3. Обновить код
git fetch origin
git reset --hard origin/master
git pull origin master

# 4. ВАЖНО! Пересобрать образы
docker compose build --no-cache

# 5. Запустить заново
docker compose up -d

# 6. Проверить логи
docker logs --tail 50 mtproxy-node-agent
```

### Вариант 3: Быстрое решение (если git pull не работает)

```bash
cd /root/eeee
docker compose down

# Удалить node-agent/src/api.ts
rm node-agent/src/api.ts

# Скачать новую версию напрямую
curl -o node-agent/src/api.ts \
  https://raw.githubusercontent.com/goodboy34-tech/eeee/master/node-agent/src/api.ts

# Пересобрать
docker compose build --no-cache node-agent
docker compose up -d

# Проверить
docker logs mtproxy-node-agent
```

## Как проверить что обновилось:

```bash
# 1. Проверить контейнеры запущены
docker ps

# 2. Проверить логи node-agent (не должно быть ошибок про docker-compose.yml)
docker logs mtproxy-node-agent | grep -i "docker-compose\|socks5"

# 3. Должно быть что-то вроде:
# [SOCKS5] Updating GOST config with X accounts
# [SOCKS5] Container started successfully
```

## После обновления:

Попробуйте снова создать SOCKS5 аккаунт через бота:
1. "🔗 Управление ссылками"
2. Выбрать ноду
3. "➕ SOCKS5"
4. Отправить:
   ```
   username: test
   password: 123456
   ```

Если всё работает - должна появиться ссылка с кнопкой!

## Если не помогло:

Проверьте файл на сервере:

```bash
# Посмотреть текущую версию функции
docker exec mtproxy-node-agent cat /app/dist/api.js | grep -A 30 "updateSocks5Config"

# Должно быть что-то про:
# - execSync('docker stop mtproxy-socks5')
# - execSync('docker run ...')
# НЕ должно быть:
# - path.join
# - docker-compose.yml
# - fs.readFileSync(composeFilePath)
```

Если видите старый код - значит Docker не пересобрал образ. Попробуйте:

```bash
docker compose down
docker rmi $(docker images | grep mtproxy-node-agent | awk '{print $3}')
docker compose build --no-cache node-agent
docker compose up -d
```

## Важно!

Убедитесь что на сервере commit: **a8a3d6f** или новее:

```bash
cd /root/eeee
git log --oneline -5

# Должно быть:
# a8a3d6f Fix SOCKS5 generation issues
```

Если нет - делайте `git pull origin master`!
