# Руководство по установке

## 📋 Обзор скриптов установки

В проекте есть несколько скриптов установки. Вот их назначение:

### 1. `install.sh` — **Универсальный установщик (РЕКОМЕНДУЕТСЯ)**

**Что делает:**
- Проверяет и устанавливает зависимости (Docker, Docker Compose)
- Клонирует репозиторий
- Запускает соответствующий скрипт установки (`install-control.sh` или `install-node.sh`)

**Использование:**
```bash
# Установка Control Panel (мейн-сервер)
bash <(curl -Ls https://github.com/goodboy34-tech/tg-mtroxy/raw/master/install.sh) control

# Установка Node Agent (нода)
bash <(curl -Ls https://github.com/goodboy34-tech/tg-mtroxy/raw/master/install.sh) node
```

**Когда использовать:** Всегда! Это основной и рекомендуемый способ установки.

---

### 2. `install-control-standalone.sh` — Автономный скрипт для Control Panel

**Что делает:**
- Клонирует репозиторий
- Запускает `install-control.sh`

**Использование:**
```bash
bash <(curl -Ls https://github.com/goodboy34-tech/tg-mtroxy/raw/master/install-control-standalone.sh)
```

**Когда использовать:** Если хотите установить только Control Panel без выбора типа установки.

**Примечание:** По сути это алиас для `install.sh control`, но без проверки зависимостей.

---

### 3. `install-control.sh` — Основной скрипт установки Control Panel

**Что делает:**
- Проверяет окружение (Docker, порты, ресурсы)
- Создает `.env` файл
- Предлагает интерактивный ввод переменных
- Запускает Docker Compose

**Использование:**
```bash
# Только если репозиторий уже клонирован вручную
cd tg-mtroxy
./install-control.sh
```

**Когда использовать:** Если вы уже клонировали репозиторий вручную и хотите запустить только установку без клонирования.

---

## ✅ Рекомендуемый способ установки

### Для Control Panel (мейн-сервер):

```bash
bash <(curl -Ls https://github.com/goodboy34-tech/tg-mtroxy/raw/master/install.sh) control
```

### Для Node Agent (нода):

```bash
bash <(curl -Ls https://github.com/goodboy34-tech/tg-mtroxy/raw/master/install.sh) node
```

---

## 🔄 Сравнение скриптов

| Скрипт | Клонирует репо | Проверяет зависимости | Устанавливает зависимости | Запускает установку |
|--------|----------------|----------------------|---------------------------|---------------------|
| `install.sh control` | ✅ | ✅ | ✅ | ✅ |
| `install-control-standalone.sh` | ✅ | ❌ | ❌ | ✅ |
| `install-control.sh` | ❌ | ✅ | ✅ | ✅ |

---

## 💡 Рекомендация

**Используйте только `install.sh`** — он делает всё необходимое и является универсальным решением.

Остальные скрипты оставлены для обратной совместимости и специальных случаев.

