# 🔧 Инструкции по обновлению

## Текущая проблема
После изменения логики `/health` API (всегда возвращает healthy), нужно обновить компоненты.

## Что исправлено в коммитах:

### Commit f734e71
- ❌ Первая попытка: SOCKS5 опционален для health check
- Проблема: MTProto контейнер все еще требовался

### Commit f88c2e1  
- ✅ Node-agent API всегда возвращает `healthy`
- ✅ Улучшено отображение статуса в `/stats`

### Commit 2560a0e
- ✅ Улучшена обработка ошибок при создании MTProto
- ✅ Добавлены информативные сообщения об ошибках
- ✅ Показывается прогресс "Добавляю секрет на ноду..."

---

## 📋 Порядок обновления:

### 1. Обновить бот (control-panel)
```bash
cd /root/mtproxy/control-panel
git pull
docker compose down
docker compose up -d --build
```

**Результат**: 
- Улучшенная статистика с прогресс-барами
- Лучшая обработка ошибок при создании MTProto
- Логи ограничены 30MB

### 2. Обновить ноды (node-agent)
```bash
cd /root/mtproxy-node
mtproxy-node update
```

Или вручную:
```bash
cd /root/mtproxy-node
curl -fsSL https://raw.githubusercontent.com/goodboy34-tech/eeee/master/node-agent/src/api.ts -o node-agent/src/api.ts
docker compose down node-agent
docker compose up -d --build node-agent
```

**Результат**:
- API всегда возвращает `healthy` (если node-agent работает)
- MTProto и SOCKS5 опциональны

### 3. Проверка работы
```bash
# В боте:
/health        # Должны показаться все ноды как 🟢 Healthy
/stats         # Должна показаться статистика с прогресс-барами
```

---

## 🐛 Известные временные проблемы:

### Проблема: Ноды показываются offline в списке `/nodes`
**Причина**: Статус в БД обновляется cron-ом каждые 5 минут

**Решение 1** (Подождать):
- Подождите 5 минут - cron обновит статусы автоматически

**Решение 2** (Перезапустить бот):
```bash
cd /root/mtproxy/control-panel
docker compose restart
```

**Решение 3** (Вручную обновить статус в БД):
```bash
cd /root/mtproxy/control-panel
sqlite3 data/database.db "UPDATE nodes SET status='online' WHERE id=1;"
```

### Проблема: Бот виснет при выборе домена/IP для MTProto
**Причина**: Ошибка при вызове API node-agent (не обновлен)

**Решение**: Обновить node-agent (см. пункт 2 выше)

**Теперь показывается**:
```
❌ Ошибка при добавлении секрета:

Failed to connect to node Node-1: <детали ошибки>

Попробуйте:
• Проверить что нода доступна: /health
• Перезапустить ноду: /restart_node 1
```

---

## ✅ Проверочный лист:

После всех обновлений проверьте:

- [ ] `/health` показывает все ноды как 🟢 Healthy
- [ ] `/stats` показывает статистику с прогресс-барами  
- [ ] `/nodes` показывает ноды как 🟢 online
- [ ] Можно создать MTProto секрет (выбрать домен/IP)
- [ ] Можно создать SOCKS5 аккаунт
- [ ] MTProto ссылки работают
- [ ] SOCKS5 подключение работает

---

## 🚨 Если что-то не работает:

### Логи бота:
```bash
cd /root/mtproxy/control-panel
docker compose logs -f --tail=100
```

### Логи ноды:
```bash
cd /root/mtproxy-node
docker compose logs -f node-agent --tail=100
```

### API ноды напрямую:
```bash
curl -H "Authorization: Bearer YOUR_API_KEY" http://YOUR_NODE_IP:3000/health
```

Должно вернуть:
```json
{
  "status": "healthy",
  "uptime": 12345,
  "mtproto": {"running": false, "workers": 2},
  "socks5": {"running": false},
  "system": {"cpuUsage": 5.2, "ramUsage": 23.1, "diskUsage": 15}
}
```

Обратите внимание: 
- `status` всегда `"healthy"` (если API отвечает)
- `mtproto.running` может быть `false` (если не настроен) - это нормально
- `socks5.running` может быть `false` (если нет аккаунтов) - это нормально
