# Руководство по работе с подписками

## Что такое подписки?

Подписка - это способ сгруппировать прокси с нескольких нод и выдать доступ к ним одной ссылкой или JSON файлом.

### Преимущества:
✅ Один файл содержит все прокси  
✅ Легко управлять доступом (включить/выключить все прокси разом)  
✅ Автоматическое обновление списка прокси  
✅ Готовый JSON для импорта в Telegram  

---

## Быстрый старт

### 1. Создать подписку

```
/create_subscription Для друзей
```

Бот покажет список активных нод. Выберите ноды, которые хотите включить в подписку.

### 2. Просмотреть все подписки

```
/subscriptions
```

Покажет список всех подписок с их статусом и статистикой.

### 3. Получить детали подписки

```
/subscription 1
```

Где `1` - ID подписки. Вы получите:
- Информацию о подписке
- Список прокси (MTProto и SOCKS5)
- Готовые ссылки для копирования
- Кнопки для управления

---

## Кнопки управления

При просмотре подписки (`/subscription <id>`) доступны кнопки:

### 📥 JSON для импорта
Скачивает JSON файл, который можно импортировать в Telegram:
1. Сохраните файл `subscription_X.json`
2. В Telegram: **Settings → Advanced → Network and proxy → Import from file**
3. Выберите скачанный файл
4. Все прокси будут добавлены автоматически

### 🔄 Обновить
Обновляет список прокси в подписке. Используйте, если:
- Добавили новые секреты на ноды
- Добавили SOCKS5 аккаунты
- Хотите увидеть актуальный список

### ⏸ Деактивировать / ▶️ Активировать
Выключает или включает подписку:
- **Деактивирована**: прокси не будут отображаться (можно использовать для временного отключения доступа)
- **Активна**: прокси доступны для использования

### 🗑 Удалить
Удаляет подписку **навсегда**. Сами прокси на нодах останутся, удалится только подписка.

---

## Примеры использования

### Сценарий 1: VIP доступ на всех нодах

Вы хотите дать VIP пользователям доступ ко всем прокси на всех нодах.

```
/create_subscription VIP Полный Доступ
```
1. Выберите все ноды
2. Подтвердите создание
3. Отправьте пользователям JSON файл или ссылки

### Сценарий 2: Тестовый доступ

Даёте временный доступ к одной ноде для тестирования.

```
/create_subscription Тест на 3 дня
```
1. Выберите одну ноду
2. Отправьте пользователю ссылку
3. Через 3 дня используйте кнопку **⏸ Деактивировать**

### Сценарий 3: Географическое распределение

Создаёте подписки по регионам.

```
/create_subscription Европа
/create_subscription США
/create_subscription Азия
```
Для каждой выбираете соответствующие ноды.

---

## Автоматическая генерация ссылок

Подписка автоматически генерирует ссылки двух типов:

### 1. MTProto (tg://)
```
tg://proxy?server=proxy1.example.com&port=443&secret=dd1234...
```
- Открывается напрямую в Telegram
- Поддерживает Fake-TLS (dd секреты)

### 2. SOCKS5 (socks5://)
```
socks5://username:password@proxy1.example.com:1080
```
- Работает в любых приложениях
- Требует авторизацию

---

## JSON формат для импорта

Файл `subscription_X.json` имеет структуру:

```json
{
  "name": "VIP Доступ",
  "description": "Премиум подписка",
  "proxies": [
    {
      "_": "inputMediaProxyServer",
      "server": "proxy1.example.com",
      "port": 443,
      "secret": "dd..."
    }
  ]
}
```

Telegram автоматически распознаёт этот формат.

---

## Управление нодами в подписке

### Что входит в подписку?

Подписка включает:
- ✅ Все **активные** MTProto секреты с выбранных нод (если `include_mtproto = true`)
- ✅ Все **активные** SOCKS5 аккаунты с выбранных нод (если `include_socks5 = true`)

### Если нода оффлайн?

Прокси с оффлайн нод **не включаются** в подписку автоматически.

### Если добавили новый секрет?

Нажмите кнопку **🔄 Обновить** в подписке, чтобы увидеть новый секрет.

---

## Статистика подписок

При просмотре подписки вы видите:

```
📋 VIP Доступ

ID: 1
Статус: 🟢 Активна
Нод: 3
Прокси: 12
MTProto: ✅
SOCKS5: ✅
Обращений: 45
```

- **Нод**: сколько нод включено в подписку
- **Прокси**: общее количество прокси (MTProto + SOCKS5)
- **Обращений**: сколько раз подписку открывали (счётчик)

---

## FAQ

### Можно ли изменить ноды в подписке?

Сейчас нет. Нужно создать новую подписку. В будущих версиях добавим редактирование.

### Можно ли отключить MTProto или SOCKS5?

При создании подписки можно выбрать, какие типы прокси включать. Изменить после создания пока нельзя.

### Что будет, если удалю секрет на ноде?

При следующем обновлении подписки (кнопка **🔄**) этот секрет исчезнет из списка.

### Можно ли дать ссылку на подписку для автообновления?

В текущей версии нет. Пользователи получают статический JSON файл. Для автообновления нужен веб-сервер (API endpoint).

### Сколько подписок можно создать?

Нет ограничений. База данных SQLite поддерживает миллионы записей.

---

## Технические детали

### База данных

Подписки хранятся в таблице `subscriptions`:

| Поле | Описание |
|------|----------|
| `id` | Уникальный ID |
| `name` | Название подписки |
| `node_ids` | JSON массив ID нод `[1, 2, 3]` |
| `include_mtproto` | Включить MTProto (0/1) |
| `include_socks5` | Включить SOCKS5 (0/1) |
| `is_active` | Активна ли подписка |
| `access_count` | Счётчик обращений |

### Код

Управление подписками реализовано в:
- `src/subscription-manager.ts` - логика
- `src/bot.ts` - команды бота
- `src/database.ts` - запросы к БД

---

## Roadmap (будущие возможности)

- [ ] Редактирование подписок (изменить ноды, название)
- [ ] Публичные ссылки на подписки (автообновление)
- [ ] Ограничение срока действия подписки
- [ ] Лимит подключений на подписку
- [ ] Аналитика использования прокси
- [ ] Интеграция с платёжными системами

---

**Готово!** Теперь вы можете эффективно управлять доступом к прокси через подписки.
